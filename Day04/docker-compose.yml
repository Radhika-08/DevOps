version: "3"

services:

  web:
    build: ./app

    ports:
      - "5000:5000"

    env_file:
      - .env

  nginx:
    image: nginx:alpine

    ports:
      - "80:80"

    depends_on:
      - web
